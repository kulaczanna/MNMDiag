fun =  function(x)
{
  a1 <- x;
  a2 <- x;
  
  return(my_list)
  
}

library("stringr")
x <- 'abcdef'

l <- nchar(x)
l

substring(x, 4, 6)

x1 <- str_to_upper(x)
x2 <- str_to_upper(x)

my_list <- list(x1, x2)